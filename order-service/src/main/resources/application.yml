spring:
  application:
    name: order-service

server:
  port: 8104

management:
  endpoints:
    web:
      exposure:
        include: health
  endpoint:
    health:
      probes:
        enabled: true

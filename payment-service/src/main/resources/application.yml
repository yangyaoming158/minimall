spring:
  application:
    name: payment-service

server:
  port: 8105

management:
  endpoints:
    web:
      exposure:
        include: health
  endpoint:
    health:
      probes:
        enabled: true

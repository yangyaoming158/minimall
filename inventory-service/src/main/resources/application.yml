spring:
  application:
    name: inventory-service

server:
  port: 8103

management:
  endpoints:
    web:
      exposure:
        include: health
  endpoint:
    health:
      probes:
        enabled: true

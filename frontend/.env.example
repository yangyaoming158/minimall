# MiniMall customer frontend environment template.
# Copy to .env.local and adjust if your api-gateway runs on a different host/port.
# All API calls go through api-gateway only. Never point this at a service port.
VITE_API_BASE_URL=http://localhost:8080
